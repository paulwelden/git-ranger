# git-ranger
Git Ranger manages and syncs local Git repositories across multiple providers like GitLab and GitHub using a simple YAML config. It discovers, clones, and updates repos automatically, keeping your workspace clean and current with minimal effort.
